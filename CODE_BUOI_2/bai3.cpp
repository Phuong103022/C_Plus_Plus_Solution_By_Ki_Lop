#include <bits/stdc++.h>
using namespace std;
using ll = long long;
const int maxn = 1e7 + 1;

int main()
{
    int n;
    cin >> n;
    cout << fixed << setprecision(3) << sqrt(1.0 * n) << ' ' << fixed << setprecision(3) << cbrt(1.0 * n);
}